apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nodejs-oc

resources:
  - deployment.yaml

patches:
  - path: openshift/crc/deployment-image-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: nodejs-oc
